#include <stdio.h>

int main() {
    int n, i, largest = 1;
    int j,c=0;
    printf("Enter a number: ");
    scanf("%d", &n);

    for (i = 2; i <= n; i++) {
      //finding the factors 
        while (n % i == 0)
             {
      //If the factor is prime
            for(j=1;j<=i;j++)
             {
            c++;
             }
         if(c==2)
         largest=i;
             }
        printf("Largest Prime Factor is %d,largest");
         return 0;
    }
