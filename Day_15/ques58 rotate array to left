#include <stdio.h>

int main()
{
    int i,n,temp;
    int arr[20];
     printf("Enter a number of elements: ");
    scanf("%d", &n);

//entering the  array elements

   for(i=0;i<n;i++)
{
   printf("enter element arr[%d]",i);
  scanf("%d",arr[i]);
}
temp=arr[0];

//rotating the array to left by 1 position

printf("the array is;");
    for(i=0;i<n-1;i++)
{
arr[i]=arr[i+1];
}
arr[0]=temp;
    return 0;
}
