#include <stdio.h>

int main()
{
    int i,n,j=0,temp;
    int arr[20];
     printf("Enter a number of elements: ");
    scanf("%d", &n);

//entering the  array elements

   for(i=0;i<n;i++)
{
   printf("enter element arr[%d]",i);
  scanf("%d",&arr[i]);
}

//move zeros to end

     for (i = 0; i < n; i++) {
        if (arr[i] != 0) {
            temp = arr[i];
            arr[i] = arr[j];
            arr[j] = temp;
            j++;
      }
}
printf("the array is;");
    for(i=0;i<n;i++)
{
printf("%d",arr[i]);
}
    return 0;
}
