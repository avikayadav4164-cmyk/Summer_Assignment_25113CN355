#include <stdio.h>

int main() {
    int i,j,h=5;
  printf("enter the height ");
  scanf("%d",&h);
  for(i=1;i<=h;i++)
  {
  for(j=1;j<=i;j++)
  {
      printf("%d \t",j);
  }
  printf("\n");
}    return 0;
}
